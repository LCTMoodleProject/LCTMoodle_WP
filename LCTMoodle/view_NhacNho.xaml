<phone:PhoneApplicationPage
    x:Class="LCTMoodle.view_NhacNho"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="White">
        <Rectangle Fill="#FF004263" HorizontalAlignment="Left" Height="65" Grid.RowSpan="2" Stroke="Black" VerticalAlignment="Top" Width="480"/>
        <TextBlock HorizontalAlignment="Left" Margin="46,10,0,0" TextWrapping="Wrap" Text="NHẮC NHỞ" VerticalAlignment="Top" Height="47" Width="377" FontFamily="Segoe WP Light" FontSize="35" TextAlignment="Center"/>
        <StackPanel Margin="0,67,0,0" Visibility="{Binding BaoThucVisible}" Background="#e6e6e6">
            <Grid Height="524" Margin="5,30,0,0">
                <StackPanel Orientation="Vertical" DataContext="{Binding m_NhacNho}" Margin="0,0,5,72">
        			<TextBlock Text="Ngày" Foreground="#FF004263" FontSize="26.667" Margin="10,0,0,0"/>
        			<toolkit:DatePicker x:Name="dpk_Ngay" Background="White" Foreground="Black" Value="{Binding NgayNhacNho, Mode=TwoWay}" BorderBrush="#BFFFFFFF" />
        			<TextBlock Text="Giờ" Foreground="#FF004263" FontSize="26.667" Margin="10,0,0,0"/>
        			<toolkit:TimePicker x:Name="tpk_Gio" Foreground="Black" BorderBrush="#BFFFFFFF" Value="{Binding GioNhacNho, Mode=TwoWay}" Background="White" />
        			<TextBlock Text="Nội dung" Foreground="#FF004263" FontSize="26.667" Margin="10,0,0,0"/>
        			<TextBox x:Name="txt_NoiDung" TextWrapping="Wrap" Height="200" BorderBrush="#BFFFFFFF" Text="{Binding NoiDung, Mode=TwoWay}"/>
        		</StackPanel>
        		<Button Content="Lưu" Height="72" x:Name="btn_Luu" VerticalAlignment="Top" Width="160" Margin="0,452,0,0" Foreground="Black" BorderBrush="Black" HorizontalAlignment="Right" Command="{Binding NhacNhoCommand}"/>
        	</Grid>
        </StackPanel>
        <StackPanel Margin="5,80,5,0" Visibility="{Binding TatBaoThucVisible}" d:IsHidden="True">
            <TextBlock Text="{Binding GioBaoThuc}" Foreground="#FF222630" FontSize="93.333" TextAlignment="Center"></TextBlock>
			<TextBlock Text="{Binding NgayBaoThuc}" Foreground="#FF222630" FontSize="33.333" TextAlignment="Center"></TextBlock>
			<Button Content="Đặt lại nhắc nhở" Foreground="White" BorderBrush="#FFFF001D" Command="{Binding DatLaiCommand}" Margin="0,30,0,0" Background="#FFFF001D" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</phone:PhoneApplicationPage>